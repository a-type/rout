# games

A low-tech game "discovery service" - this just aggregates the main manifests of all games in this repo to be easily iterated by the API...

One day this would be a proper service with a database for game registration which would fetch manifests from deployed URLs.
